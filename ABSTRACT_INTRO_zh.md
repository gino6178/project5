# 空間適應的場景重定向：端到端圖條件 Transformer

> 草稿 v2 — 中文摘要與介紹。標註 **[待量測]** 者為尚未取得的數字，不得寫入正式稿。

---

## 摘要

機器人要在不同的空間裡完成相同的工作。然而，把一個既有房間的佈置搬到另一個形狀、尺寸都不同的房間，目前沒有可用的方法：生成式場景合成（如 PhyScene）只能針對資料集既有的房型「重新生成」一個場景，產生的結果與原始房間毫無關係，機器人面對的等於是一組全新的語意配置；而幾何變換（仿射映射）雖然完整保留了物件關係，卻因為家具是剛體、目標邊界不是來源房間的等比縮放，導致物件互相穿透、甚至卡在牆外（真實跨房間邊界上有 9.7% 的物件落在房間之外），機器人無法使用。

我們把這個問題定義為**空間適應的場景重定向**：給定來源房間的佈置與其設計關係圖，以及一個任意形狀與尺寸的目標房間，輸出一個在新房間中物理可行、且語意與來源一致的佈置。我們提出一個**端到端的圖條件 Transformer**：設計關係圖、來源佈局與目標房間邊界一同作為輸入，約束的滿足在 Transformer 內部以迭代精修完成，**輸出即為最終佈局，不含任何後處理或外掛的最佳化步驟**。

在同一批房間、同一個評測器下，我們的結果在物件碰撞率（0.129 對 PhyScene 的 0.409）與物件可達率（0.886 對 0.863）上優於 PhyScene，並且遠優於仿射映射（出界率 0.4% 對 9.7%）；可走區域比例則略低於 PhyScene（0.900 對 0.963），我們如實報告此一取捨。我們並直接量測了 PhyScene 在偏離資料集分布的房型下的行為：在分布內的矩形房間，它生成的物件**全部落在房內（出界率 0.0%）**；換成 L 形、T 形、梯形或 1:4 狹長走廊，出界率升到 **8.9%–20.8%**，碰撞率亦由 0.536 升至 0.616。這量化了「既有生成模型的房型條件綁在資料集分布上」這一前提。

我們的主張限於**產生器層級的保證**——環境可導航、語意一致、物理有效——不宣稱以此訓練的機器人策略更強；後者需要模擬器與策略訓練，非本文範圍。

---

## 1. 介紹

### 1.1 問題

一個在客廳訓練過的服務型機器人，換到另一間客廳時，面對的是完全不同的空間。理想上我們希望它面對的是「**同一套佈置，只是換了房間**」——沙發仍然對著電視、餐椅仍然圍著餐桌、目標物仍然在它預期的功能位置上——這樣它的空間先驗才能遷移。要做到這件事，需要把一個房間的佈置搬到另一個房間，同時滿足兩件互相衝突的要求：

- **語意一致**：物件組成與功能關係（motif：餐桌與餐椅、床與床頭櫃）必須保留。這是相對的、對尺度敏感的性質。
- **物理可行**：在新的邊界內不得碰撞、不得出界、貼牆物件要貼齊。這是絕對的、由邊界決定的性質。

### 1.2 現有方法為何不適用

**（一）生成式場景合成**。ATISS、DiffuScene 一類的模型從先驗分布取樣，PhyScene 進一步把碰撞、可達性等物理約束以引導的方式加入擴散過程，是目前最接近「為機器人生成可用場景」的工作。但它們解的是**無條件生成**：給定一個房間，生成一個合理的佈置。它們沒有機制把**某一個特定來源房間**的佈置綁定到新的邊界上——生成的結果與來源無關，機器人得到的是新的語意配置，而不是同一套佈置的空間變體。

此外，這類模型的房型條件來自資料集既有的樓板平面。PhyScene 把房型編碼成 256×256 的光柵遮罩再送進 CNN 編碼器，學到的是 3D-FRONT 樓板平面的**分布**；一旦房型離開該分布，條件嵌入即失效。我們以其官方權重直接量測（表 1）：分布內的矩形房間出界率為 **0.0%**，而 L 形升到 **20.8%**、T 形 14.9%、1:4 走廊 16.7%、梯形 8.9%，碰撞率同時由 0.536 升到 0.616。值得注意的是，PhyScene 隨附的引導設定中 `room_layout`（把物件推回房內的引導）與 `reachability` **預設為關閉**，因此出界率的退化並非引導設定所能補救。

**（二）幾何變換**。把來源佈局以仿射映射搬進目標房間，是最直接的做法。因為映射是線性的，相對結構與 motif 完整保留——我們實測仿射的 motif 保留分數為 1.000。但它的失效不在關係層面而在物理層面：家具是剛體，而目標邊界不是來源的等比副本，於是房間壓縮時物件互相重疊（4.9% 的物件對碰撞），不規則邊界上物件穿牆而出（真實跨房間配對下 9.7% 出界），而且線性映射無法在較小的房間裡刪去放不下的物件。對機器人而言，這樣的場景不可用。

**（三）後處理修正**。在生成之後外掛一個最佳化或啟發式吸附來修復幾何，是常見的補救。但多步的測試期最佳化緩慢且容易陷入局部極值，而硬性吸附會把物件推離生成流形，破壞剛剛才保住的關係結構並引入二次碰撞。更根本的是，它讓系統不再是端到端的：模型被訓練去優化一個目標，部署時卻套用另一個。

### 1.3 我們的做法

我們提出一個**單一的圖條件 Transformer**，直接輸出可用的佈局：

- **圖作為一級輸入**。來源房間的設計關係圖（物件為節點，功能關係為邊，含關係的彈性係數）與佈局、目標邊界一同編碼進 Transformer。關係保留因此是模型的條件，而非事後的懲罰項。
- **約束優化在 Transformer 內部**。可行性（不碰撞、不出界、貼牆）以迭代精修的方式在網路層內完成，而非交給外部的投影或最佳化器。模型訓練與部署的目標因此一致。
- **無後處理**。網路的輸出即為最終場景。

### 1.4 貢獻

1. 把**空間適應的場景重定向**定義為一個任務：任意目標房型 + 來源設計保留 + 物理可行，並說明它與無條件場景生成、與環境隨機化（如 digital cousins 只更換資產、保持房間不變）的區別。
2. 一個**端到端、無後處理**的圖條件 Transformer，約束滿足內建於網路。
3. 在 PhyScene 的評測協定下的量化比較，並以其官方權重量化其在非資料集房型下的退化（表 1）。
4. 明確的範圍聲明：本文證明產生器層級的保證，不宣稱下游策略效能。

---

## 表 1：PhyScene 在非分布房型下的退化

以官方 livingroom 權重生成，每種房型 12 個場景，同一量測程序。

| 目標房型 | 出界率 ↓ | 碰撞率 ↓ |
|---|---|---|
| 矩形（分布內） | **0.000** | 0.536 |
| 梯形（斜牆） | 0.089 | 0.542 |
| T 形（雙凹角） | 0.149 | 0.589 |
| **L 形（深角切）** | **0.208** | 0.595 |
| 1:4 狹長走廊 | 0.167 | **0.616** |

分布內房型的出界率為零，離開分布後升至 9–21%：房型條件是綁在資料集分布上的。

## 待補與風險

- ✅ **已完成**：PhyScene 在 L 形 / T 形 / 梯形 / 狹長走廊下的退化(§1.2 一、表 1)。**誠實界線**：(a) 絕對數值來自我們的合成房型與軸對齊足跡指標，**不可與 PhyScene 原論文的數字並列**，有效結論是同一量測下的**跨房型相對退化**；(b) 其 mesh 級碰撞引導需要 3D-FUTURE 網格資產（44 GB，未下載），故該項引導未啟用——但其隨附設定中負責房內containment 的 `room_layout` 引導本就預設關閉，出界率結論不受影響。
- **[待量測]** PhyScene 生成場景對來源房間的語意保留（預期接近 0，因為它不試圖保留）——這張表展示的是**能力缺口**而非分數高低。
- ✅ **已定案**：模型為 **Graph-Refinement Transformer**（`DESIGN.md`）。六個殘差精修塊，每塊在網路內重算違規特徵並學習修正；邊特徵同時調變注意力 logits 與訊息；**「什麼算違規」由 `PairGapPredictor` 從真實佈局學習**，而非規則表——它自行學到餐桌–餐椅（有邊）期望間距 −0.37 m（該重疊）、無關物件 +1.75 m（該分開）。先前以外掛投影（即使摺入訓練）的版本已捨棄，不符無後處理要求。訓練進行中。
- 可走區域比例在 project4 低於 PhyScene（0.900 對 0.963）。project5 已加入**可微光柵化的 blocked-walkway 懲罰**直接優化此軸（`walkable.py`；驗證：整道牆切斷房間 0.430、同一道牆留 1.2 m 門縫 0.018、暢通 0.006），成效待訓練後量測。
